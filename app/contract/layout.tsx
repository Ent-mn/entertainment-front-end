import React from 'react';

export const metadata = {
  title: 'Contract | Restaurant.mn',
  description: 'Contract management page for Restaurant.mn',
};

export default function ContractLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
} 